---
name: html-gallery
description: Vite MPA entry HTML for the cross-case image gallery page — loads main-gallery.tsx into #root
metadata:
  type: reference
---

# frontend/gallery.html

**หน้าที่**: ไฟล์ HTML entry ของหน้าคลังภาพ — โหลด `main-gallery.tsx` build แล้วกลายเป็น `static/gallery.html`

**Functions/Variables (global scope)**: ไม่มี

**Called by**: [[html-index]]/[[components-Navbar]] ("คลังภาพ")

**Depends on**: [[main-gallery]], build โดย [[vite-config]] เป็น entry ชื่อ `gallery`
