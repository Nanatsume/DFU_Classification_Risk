---
name: main-crf-list
description: Vite entry script that mounts CrfList inside AuthGuard onto #root of crf-list.html
metadata:
  type: reference
---

# frontend/src/main-crf-list.tsx

**หน้าที่**: entry script ของ `crf-list.html` — render `<AuthGuard><CrfList /></AuthGuard>`

**Functions/Variables (global scope)**: ไม่มี

**Called by**: `crf-list.html` (ดู [[html-crf-list]])

**Depends on**: [[lib-auth]] (`AuthGuard`), [[pages-CrfList]], `frontend/src/index.css` ([[index-css]])
