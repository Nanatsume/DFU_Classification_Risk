---
name: main-gallery
description: Vite entry script that mounts Gallery inside AuthGuard onto #root of gallery.html
metadata:
  type: reference
---

# frontend/src/main-gallery.tsx

**หน้าที่**: entry script ของ `gallery.html` — render `<AuthGuard><Gallery /></AuthGuard>`

**Functions/Variables (global scope)**: ไม่มี

**Called by**: `gallery.html` (ดู [[html-gallery]])

**Depends on**: [[lib-auth]] (`AuthGuard`), [[pages-Gallery]], `frontend/src/index.css` ([[index-css]])
