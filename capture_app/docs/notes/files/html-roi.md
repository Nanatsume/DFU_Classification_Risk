---
name: html-roi
description: Vite MPA entry HTML for the ROI case picker page — loads main-roi.tsx into #root
metadata:
  type: reference
---

# frontend/roi.html

**หน้าที่**: ไฟล์ HTML entry ของหน้าเลือกเคสทำ ROI — โหลด `main-roi.tsx` build แล้วกลายเป็น `static/roi.html`

**Functions/Variables (global scope)**: ไม่มี

**Called by**: [[html-index]]/[[components-Navbar]] ("ทำ ROI")

**Depends on**: [[main-roi]], build โดย [[vite-config]] เป็น entry ชื่อ `roi`
