---
name: main-crf-detail
description: Vite entry script that mounts CrfDetail inside AuthGuard onto #root of crf-detail.html
metadata:
  type: reference
---

# frontend/src/main-crf-detail.tsx

**หน้าที่**: entry script ของ `crf-detail.html` — render `<AuthGuard><CrfDetail /></AuthGuard>`

**Functions/Variables (global scope)**: ไม่มี

**Called by**: `crf-detail.html` (ดู [[html-crf-detail]])

**Depends on**: [[lib-auth]] (`AuthGuard`), [[pages-CrfDetail]], `frontend/src/index.css` ([[index-css]])
