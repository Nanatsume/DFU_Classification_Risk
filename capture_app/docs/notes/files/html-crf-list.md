---
name: html-crf-list
description: Vite MPA entry HTML for the case history table page — loads main-crf-list.tsx into #root
metadata:
  type: reference
---

# frontend/crf-list.html

**หน้าที่**: ไฟล์ HTML entry ของหน้าประวัติการบันทึก — โหลด `main-crf-list.tsx` build แล้วกลายเป็น `static/crf-list.html`

**Functions/Variables (global scope)**: ไม่มี

**Called by**: [[html-index]]/[[components-Navbar]] ("ประวัติการบันทึก"), [[html-crf-detail]] ("กลับไปหน้าประวัติ")

**Depends on**: [[main-crf-list]], build โดย [[vite-config]] เป็น entry ชื่อ `crf-list`
